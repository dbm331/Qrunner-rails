Click below to reset your password

<%= edit_password_reset_url(@user.password_reset_token) %>

If you didn't request this, just ignore it, your password will remain the same and this reset token will expire in a few minutes.